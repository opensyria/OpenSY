# دليل التعدين - أوبن سي 🇸🇾

## مقدمة

التعدين في أوبن سي سهل ومتاح للجميع! نستخدم خوارزمية RandomX التي تعمل بشكل مثالي على المعالجات العادية.

---

## المتطلبات

### الحد الأدنى

| المكون | المتطلب |
|--------|---------|
| **المعالج** | أي معالج حديث (4 أنوية+) |
| **الذاكرة** | 4 جيجابايت RAM |
| **التخزين** | 10 جيجابايت مساحة فارغة |
| **الإنترنت** | اتصال مستقر |

### الموصى به

| المكون | المتطلب |
|--------|---------|
| **المعالج** | AMD Ryzen أو Intel Core (8+ أنوية) |
| **الذاكرة** | 8+ جيجابايت RAM |
| **التخزين** | SSD 50+ جيجابايت |
| **الإنترنت** | اتصال سريع ومستقر |

---

## خطوات التعدين

### الخطوة 1: تثبيت البرنامج

```bash
# استنساخ المستودع
git clone https://github.com/opensyria/OpenSY.git
cd OpenSY

# البناء
cmake -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j$(nproc)
```

### الخطوة 2: تشغيل العقدة

```bash
# تشغيل العقدة في الخلفية
./build/bin/opensyd -daemon

# التحقق من حالة المزامنة
./build/bin/opensy-cli getblockchaininfo
```

انتظر حتى تتم المزامنة الكاملة (قد تستغرق ساعات).

### الخطوة 3: إنشاء محفظة

```bash
# إنشاء محفظة للتعدين
./build/bin/opensy-cli createwallet "تعدين"

# الحصول على عنوان للتعدين
./build/bin/opensy-cli getnewaddress "مكافآت"
```

احفظ العنوان الذي يظهر! سيبدأ بـ `F` أو `syl1`.

### الخطوة 4: بدء التعدين

```bash
# تعدين 100 كتلة إلى عنوانك
./build/bin/opensy-cli generatetoaddress 100 YOUR_ADDRESS
```

استبدل `YOUR_ADDRESS` بالعنوان الذي حصلت عليه.

---

## نصائح للتعدين الفعال

### 1. استخدم جميع الأنوية

```bash
# التحقق من عدد الأنوية
nproc

# التعدين يستخدم جميع الأنوية تلقائياً
```

### 2. راقب درجة الحرارة

التعدين يستهلك المعالج بالكامل. تأكد من:
- تهوية جيدة للحاسوب
- درجة حرارة المعالج أقل من 85°C

### 3. التعدين المستمر

```bash
# سكربت للتعدين المستمر
while true; do
    ./build/bin/opensy-cli generatetoaddress 10 YOUR_ADDRESS
done
```

---

## الأسئلة الشائعة

### كم سأربح من التعدين؟

يعتمد على:
- قوة معالجك
- إجمالي قوة الشبكة
- حظك في إيجاد الكتل

حالياً، مكافأة كل كتلة هي **10,000 SYL**.

### متى أستلم المكافآت؟

بعد تعدين كتلة، تحتاج **100 تأكيد** (~3.3 ساعات) قبل أن تتمكن من إنفاق المكافأة.

### هل يمكنني التعدين على الهاتف؟

لا ننصح بذلك. RandomX مصمم للمعالجات الكاملة وسيكون بطيئاً جداً على الهواتف.

### ماذا لو انقطع الإنترنت؟

ستتوقف عن التعدين مؤقتاً. عند عودة الاتصال، ستستأنف العقدة تلقائياً.

---

## استكشاف الأخطاء

### "Node is still syncing"

انتظر حتى تكتمل المزامنة:
```bash
./build/bin/opensy-cli getblockchaininfo | grep "blocks"
./build/bin/opensy-cli getblockchaininfo | grep "headers"
```

عندما يتساوى `blocks` و `headers`، المزامنة مكتملة.

### "Wallet not found"

تأكد من إنشاء محفظة أولاً:
```bash
./build/bin/opensy-cli createwallet "تعدين"
```

### "Connection refused"

تأكد من تشغيل العقدة:
```bash
./build/bin/opensyd -daemon
```

---

## المعالجات الموصى بها

| المعالج | الأداء المتوقع |
|---------|----------------|
| AMD EPYC 7742 | ممتاز (64 نواة) |
| AMD Ryzen 9 5950X | ممتاز (16 نواة) |
| Intel Xeon Gold | جيد جداً |
| AMD Ryzen 7 5800X | جيد (8 أنوية) |
| Intel Core i7 | جيد (8 أنوية) |
| AMD Ryzen 5 | مقبول (6 أنوية) |
| Intel Core i5 | مقبول (6 أنوية) |

---

## الدعم

إذا واجهت مشاكل:
1. راجع الأسئلة الشائعة أعلاه
2. ابحث في GitHub Issues
3. افتح Issue جديد مع تفاصيل المشكلة

---

<p align="center">
  <strong>تعدين سعيد! ⛏️</strong>
</p>
