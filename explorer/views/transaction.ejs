<!DOCTYPE html>
<html lang="<%= lang %>" dir="<%= dir %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.transaction %> | <%= t.siteName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <% if (dir === 'rtl') { %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <% } %>
    <style>
        :root { --syria-green: #007A3D; --syria-red: #CE1126; }
        body { background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%); color: #e8e8e8; min-height: 100vh; }
        .navbar { background: rgba(0, 122, 61, 0.95) !important; border-bottom: 3px solid var(--syria-red); }
        .navbar-brand { font-weight: bold; font-size: 1.4rem; }
        .card { background: rgba(255, 255, 255, 0.05); border: 1px solid rgba(255, 255, 255, 0.1); }
        .card-header { background: rgba(0, 122, 61, 0.3); border-bottom: 1px solid rgba(255, 255, 255, 0.1); }
        .table { color: #e8e8e8; }
        a { color: #60a5fa; text-decoration: none; }
        a:hover { color: #93c5fd; }
        .hash-link { font-family: monospace; font-size: 0.85rem; }
        .flag-banner { height: 6px; background: linear-gradient(to right, var(--syria-green) 0%, var(--syria-green) 33%, #FFF 33%, #FFF 66%, #000 66%, #000 100%); }
        .footer { background: rgba(0, 0, 0, 0.3); padding: 2rem 0; margin-top: 3rem; }
        .list-group-item { background: transparent; border-color: rgba(255,255,255,0.1); color: #e8e8e8; }
    </style>
</head>
<body>
    <div class="flag-banner"></div>
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="/">🇸🇾 <%= t.siteName %></a>
            <a href="?lang=<%= lang === 'ar' ? 'en' : 'ar' %>" class="btn btn-outline-light btn-sm ms-auto"><%= t.switchLang %></a>
        </div>
    </nav>

    <main class="container my-4">
        <nav aria-label="breadcrumb" class="mb-4">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/"><%= t.home %></a></li>
                <li class="breadcrumb-item active"><%= t.transaction %></li>
            </ol>
        </nav>

        <div class="card mb-4">
            <div class="card-header">
                <h4 class="mb-0"><i class="bi bi-arrow-left-right"></i> <%= t.transactionDetails %></h4>
            </div>
            <div class="card-body">
                <table class="table table-sm">
                    <tr><td><strong><%= t.txid %></strong></td><td class="hash-link text-break"><%= tx.txid %></td></tr>
                    <tr>
                        <td><strong><%= t.status %></strong></td>
                        <td>
                            <% if (tx.confirmations > 0) { %>
                            <span class="badge bg-success"><%= t.confirmed %></span> (<%= tx.confirmations %> <%= t.confirmations %>)
                            <% } else { %>
                            <span class="badge bg-warning"><%= t.pending %></span>
                            <% } %>
                        </td>
                    </tr>
                    <% if (tx.blockhash) { %>
                    <tr><td><strong><%= t.inBlock %></strong></td><td><a href="/block/<%= tx.blockhash %>" class="hash-link"><%= tx.blockhash %></a></td></tr>
                    <% } %>
                    <tr><td><strong><%= t.size %></strong></td><td><%= tx.size %> <%= t.bytes %></td></tr>
                </table>
            </div>
        </div>

        <div class="row">
            <div class="col-md-6 mb-4">
                <div class="card h-100">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi bi-box-arrow-in-right"></i> <%= t.inputs %> (<%= tx.vin.length %>)</h5>
                    </div>
                    <div class="card-body p-0">
                        <div class="list-group list-group-flush">
                            <% tx.vin.forEach(function(input, i) { %>
                            <div class="list-group-item">
                                <small class="text-muted">#<%= i + 1 %></small>
                                <% if (input.coinbase) { %>
                                <span class="badge bg-success ms-2"><%= t.coinbase %></span>
                                <% } else { %>
                                <a href="/tx/<%= input.txid %>" class="hash-link small ms-2"><%= input.txid.substring(0, 24) %>...</a>
                                <small class="text-muted">:<%= input.vout %></small>
                                <% } %>
                            </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-md-6 mb-4">
                <div class="card h-100">
                    <div class="card-header">
                        <h5 class="mb-0"><i class="bi bi-box-arrow-right"></i> <%= t.outputs %> (<%= tx.vout.length %>)</h5>
                    </div>
                    <div class="card-body p-0">
                        <div class="list-group list-group-flush">
                            <% tx.vout.forEach(function(output, i) { %>
                            <div class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <small class="text-muted">#<%= i %></small>
                                    <% if (output.scriptPubKey.address) { %>
                                    <a href="/address/<%= output.scriptPubKey.address %>" class="hash-link ms-2"><%= output.scriptPubKey.address %></a>
                                    <% } else { %>
                                    <span class="text-muted ms-2"><%= output.scriptPubKey.type %></span>
                                    <% } %>
                                </div>
                                <span class="badge bg-info"><%= output.value.toFixed(8) %> <%= coinSymbol %></span>
                            </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container text-center">
            <p class="mb-2"><strong><%= coinName %> (<%= coinSymbol %>)</strong> - <%= t.siteTagline %></p>
            <p class="mb-0">سوريا حرة 🇸🇾 Free Syria</p>
        </div>
    </footer>
</body>
</html>
